node_modules/
build/
dist/
coverage/
# Playwright E2E artifacts
test-results/
playwright-report/
blob-report/
.DS_Store
*.log
.env*
!.env.example
nul
*.tmp
*.bak

# Local AI-workflow scratch notes (keep on disk for later sessions, never commit)
Claude-AUDIT

# Firebase client config (even if keys are "public", do not commit raw for best practice + quota protection)
firebase-applet-config.json
firebase-debug.log
.firestore-debug.log
functions/lib/
.firebase/
